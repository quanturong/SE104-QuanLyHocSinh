diff --git a/node_modules/connect-flash/lib/flash.js b/node_modules/connect-flash/lib/flash.js
index a278bc1..95cede8 100644
--- a/node_modules/connect-flash/lib/flash.js
+++ b/node_modules/connect-flash/lib/flash.js
@@ -2,7 +2,8 @@
  * Module dependencies.
  */
 var format = require('util').format;
-var isArray = require('util').isArray;
+// util.isArray is deprecated; use Array.isArray instead to avoid DeprecationWarning
+var isArray = Array.isArray;
 
 
 /**
@@ -65,8 +66,12 @@ function _flash(type, msg) {
       var args = Array.prototype.slice.call(arguments, 1);
       msg = format.apply(undefined, args);
     } else if (isArray(msg)) {
-      msg.forEach(function(val){
-        (msgs[type] = msgs[type] || []).push(val);
+      // Ensure the array exists even if `msg` is empty to avoid TypeError when
+      // returning length (previous code attempted to access length of undefined
+      // when an empty array was passed).
+      msgs[type] = msgs[type] || [];
+      msg.forEach(function(val) {
+        msgs[type].push(val);
       });
       return msgs[type].length;
     }
